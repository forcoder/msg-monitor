# 2026-04-06

- 使用本机 JDK 17（`C:\Users\13880\.jdks\ms-17.0.18`）和 Android SDK（`D:\Android\SDK`）成功执行 `gradlew.bat assembleDebug`，产出可安装的 Debug APK：`app/build/outputs/apk/debug/app-debug.apk`。
- 为恢复构建，已将 Android namespace 切换为 `com.csbaby.kefu`，修正 Manifest 组件注册，修复 `AIService`、`KnowledgeBaseManager`、`ReplyOrchestrator`、`ProfileScreen`、`ModelScreen` 的编译问题，并将旧原型源码从 `app/src/main/java/com/kefu/xiaomi` 移到 `app/_disabled_sources/com/kefu/xiaomi` 以避免参与编译。
- 修复微信悬浮回复不显示问题：将 `NotificationListenerServiceImpl` 直接桥接到 `MessageMonitor` 单例，由 `KefuApplication` 自动启动 `ReplyOrchestrator` 并初始化知识库匹配器，默认补齐微信包名 `com.tencent.mm` 的监控配置，同时把 `FloatingWindowService` 从占位实现改成真实可见的原生悬浮视图，可展示客户消息与建议回复。
- 为悬浮窗增加“直接发送”按钮：新增 `ChatAutomationAccessibilityService` 无障碍服务，悬浮窗会把目标包名与会话标题一并保存，点击后可将建议回复自动写入当前微信聊天输入框并触发发送；若未授予无障碍权限或发送失败，会自动复制回复内容作为兜底。已再次通过 `gradlew.bat assembleDebug` 验证构建成功。
- 排查“大模型配置/规则库是否落本地”时确认：当前两者都已通过 Room 持久化到本地数据库 `kefu_database`，其中模型表为 `ai_model_configs`，规则表为 `keyword_rules`，规则场景关联表为 `rule_scenario_relation`；DataStore `kefu_settings` 负责保存监控开关、默认模型 ID 等偏好项。
- 知识库新增 `.json` 导入入口：在 `KnowledgeScreen` 顶栏接入系统文件选择器，选中文件后交给 `KnowledgeBaseManager.importFromJson()` 导入；同时把规则模型/Room 实体扩展出 `targetType`、`targetNames`，支持按“全部会话 / 指定个人 / 指定群组”配置，并在通知监听中补充会话标题与群聊标记，让 `ReplyGenerator` 只命中当前聊天对象适用的规则。已重新通过 `gradlew.bat assembleDebug` 生成可测试 APK。
- 修复外部旧规则文件导入失败：`KnowledgeBaseManager.importFromJson()` 现在支持逐行 JSON（NDJSON/JSON Lines）以及 `trigger_condition` / `reply_content` / `status` 这类第三方字段；对“关键字: ...”和“咨询问题: ...”会自动拆分成多条本地关键词规则。已使用 JDK 17 重新执行 `gradlew.bat assembleDebug`，构建通过。
- 增强建议回复体验：悬浮窗现在会在建议标题和元信息里用 `🤖 AI` / `📚 规则` 图标区分来源；同时取消“消息一到就提前学习”的旧逻辑，改成只有在悬浮窗自动发送成功后，才把用户实际采用的回复写入 `ReplyHistory` 并交给 `StyleLearningEngine` 学习。风格学习新增本地特征分析（正式度、热情度、专业度、长度、常用短语）并结合最近回复做 AI 深度分析。已再次通过 `gradlew.bat assembleDebug` 验证构建成功。
- 已将 `C:\Users\13880\WorkBuddy\Claw\百居易消息自动化规则_fixed.json` 离线标准化为 `d:\workspace\workbuddy\csBaby\百居易消息自动化规则_standardized_v2.json`：输出采用本地标准导入结构 `version/exportTime/rules`，并把可解析的“关键字/咨询问题”规则展开为 780 条标准 `KeywordRule`；20 条订单/兜底类事件规则因当前知识库仅支持关键词规则，未纳入标准化导出。
- 悬浮回复窗已升级为“默认小圆球 + 点击展开炫酷面板”模式：`FloatingWindowService` 现在默认只显示可拖动的渐变悬浮图标，点击后展开深色玻璃感面板，顶部支持收起/关闭，底部保留复制与直接发送；新增矢量图标 `app/src/main/res/drawable/ic_kefu_orb.xml` 供悬浮球和通知复用，并已用 JDK 17 再次通过 `gradlew.bat assembleDebug` 验证构建成功。
- 已于 2026-04-06 晚间再次使用本机 JDK 17 重新执行 `gradlew.bat assembleDebug`，最新 Debug APK 仍输出到 `app/build/outputs/apk/debug/app-debug.apk`，可直接安装验证当前悬浮窗与直接发送功能。
























































