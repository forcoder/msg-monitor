# 2026-04-07

- 已把百居易 Android 应用 `com.myhostex.hostexapp` 接入默认监听集合：`PreferencesManager.DEFAULT_MONITORED_APPS`、`NotificationListenerServiceImpl` 回落逻辑以及 `KefuApplication` 启动初始化都会默认覆盖微信与百居易，新消息会继续走 `MessageMonitor -> ReplyOrchestrator -> FloatingWindowService` 自动生成建议回复；同时无障碍服务配置和运行时 `packageNames` 也已扩到这两个应用，悬浮窗头部改成“大收起按钮 + 小关闭按钮”的图标化样式，并重新通过 `gradlew.bat assembleDebug` 生成最新 `app/build/outputs/apk/debug/app-debug.apk`。
- 已修复“直接发送”常见的 `INPUT_NOT_FOUND` 场景：`ChatAutomationAccessibilityService` 现在不再只认 `EditText/isEditable`，而是结合 `ACTION_SET_TEXT`、viewId/hint/描述关键词、发送按钮附近节点等多重特征来定位聊天输入框，并放宽发送按钮识别到 viewId 关键词；失败时会输出精简无障碍树日志，便于继续适配百居易这类自定义聊天界面，最新版 APK 已再次通过 `gradlew.bat assembleDebug` 验证。
- 已把美团民宿 `com.meituan.phoenix` 与途家民宿 `com.tujia.hotel` 接入默认监听应用集合：`PreferencesManager.DEFAULT_MONITORED_APPS` 现会和微信、百居易一起写入已选应用，通知监听与 `ReplyOrchestrator` 的消息过滤会自动覆盖这四个 App；无障碍服务的静态 `packageNames` 与运行时支持包列表也已同步扩展，并重新通过 `gradlew.bat assembleDebug` 生成最新 `app/build/outputs/apk/debug/app-debug.apk`。
- 已完成一轮较大的交互与规则能力升级：悬浮窗建议回复改为可直接编辑的多行输入框，头部动作改成更大的收起图标 + 右上角关闭打叉图标，并尝试通过 `FLAG_WATCH_OUTSIDE_TOUCH` 在点到面板外时自动收起；直接发送前会临时让出悬浮层焦点，`ChatAutomationAccessibilityService` 也会从 `flagRetrieveInteractiveWindows` 提供的窗口列表里优先寻找目标聊天窗口，减少百居易因为悬浮层抢焦点而报 `未找到聊天输入框` 的概率。知识库导入现已支持 JSON/CSV 两种格式、`PROPERTY` 房源级规则与当前房源优先排序，并把“逗号分隔多关键词”保留在同一条规则里，匹配到任一关键词即返回该规则，不再拆成多条。
- 已进一步针对百居易 `com.myhostex.hostexapp`、美团民宿 `com.meituan.phoenix`、途家民宿 `com.tujia.hotel` 强化“直接发送”适配：`ChatAutomationAccessibilityService` 现在会基于平台画像给输入框/发送按钮打分，优先选择带聊天语义的底部节点、必要时先点击唤起输入区，再结合输入框邻近关系识别右侧或下方发送按钮；窗口解析也会在交互窗口列表中选出最像聊天页的根节点，并已重新通过 `gradlew.bat assembleDebug` 验证构建成功。
- 已为知识库增加“清空”能力：知识库页顶栏新增带确认弹窗的一键清空入口，`KnowledgeBaseManager -> KeywordRuleRepository -> KeywordRuleDao/ScenarioDao` 会同步删除 `keyword_rules` 与 `rule_scenario_relation` 中的全部规则关联，但保留场景配置本身；界面也补了清空进度提示、搜索无结果/知识库为空两种空态文案，并重新通过 `gradlew.bat assembleDebug` 验证构建成功。
- 已修复百居易新消息偶发不出建议回复的问题：`NotificationListenerServiceImpl` 现在会额外解析 `Notification.EXTRA_MESSAGES`（MessagingStyle）和 `Notification.EXTRA_TEXT_LINES`（InboxStyle）里的最新正文，并在正文仍为空时输出明确跳过日志；`ReplyOrchestrator.extractPropertyName()` 也改成优先用 `conversationTitle` 提取百居易房源名，避免把客户昵称误当房源导致对应规则命不中。最新代码已重新通过 `gradlew.bat assembleDebug` 验证构建成功。
- 已新增百居易专用细粒度调试日志：通知层会记录正文来源（`messages` / `text_lines` / `text` / `big_text` / `ticker`）及原始候选字段，编排层会记录消息接收、房源名解析、知识库/AI命中结果和悬浮窗转发，悬浮窗层也会记录展示请求；同时知识库规则编辑器已把“适用对象”简化为“适用房源”，默认就是全部房源，也可改成指定房源并输入多个房源名称。最新代码已重新通过 `gradlew.bat assembleDebug` 验证构建成功。
- 已为首页补上监控应用管理能力：现在可直接查看并勾选/取消微信、百居易、美团民宿、途家民宿，选择结果持久化到 `kefu_settings.selected_apps`，且取消勾选后不会再被 `KefuApplication` 或通知/编排层自动补回默认值；同时首页知识库统计已改为订阅 `KeywordRuleDao.getRuleCountFlow()` 实时刷新，并通过 `gradlew.bat assembleDebug` 验证构建成功。
- 已修正知识库导入时 `applicable_properties` 的语义：`KnowledgeBaseManager.importFromJson()` 与 `importFromCsv()` 现在都会把该字段按“适用房源”解析，优先落到 `targetType=PROPERTY` + `targetNames`，同时兼容旧的 `target_names`/固定列 CSV，并已通过 `gradlew.bat assembleDebug` 验证构建成功。
- 已为知识库新增 Excel `.xlsx` 导入：`KnowledgeBaseManager.importFromExcel()` 会解析工作簿工作表并复用现有表头/旧字段映射，`KnowledgeViewModel` 会按 MIME/扩展名区分 JSON、CSV、`.xlsx` 与旧版 `.xls`（后者提示先另存为 `.xlsx`）；同时知识库列表卡片已改成独立高亮展示“适用房源”，并通过 `gradlew.bat assembleDebug` 验证构建成功。
- 已补充 Excel 首行表头兼容：知识库导入时，若 `.xlsx` 第一行使用“适用房源”或旧导出里的 `category` 列，都会按房源目标导入到 `targetType=PROPERTY` + `targetNames`；若需要单独传规则分类，可使用 `rule_category` / `规则分类`，并已再次通过 `gradlew.bat assembleDebug` 验证。
- 已补充 Excel 旧字段映射：表头为“回复内容”时会导入到规则 `replyTemplate`，表头为“规则标题”时会先去掉 `关键字: ` / `关键词: ` 前缀再导入到规则关键词；相关改动已通过 `gradlew.bat assembleDebug` 验证。












































































