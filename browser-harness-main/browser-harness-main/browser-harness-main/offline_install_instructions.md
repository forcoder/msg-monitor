无法通过直接网络连接安装browser-harness。

步骤：
1. 在可以访问GitHub的机器上执行：
git clone https://github.com/browser-use/browser-harness.git

2. 将整个项目复制到当前环境（如U盘、内部网盘、邮件附件等）

3. 解压后进入目录：
cd browser-harness

4. 安装依赖（如有virtualenv该启用）：
pip install -e .