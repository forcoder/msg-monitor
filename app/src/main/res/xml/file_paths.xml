<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- External files directory -->
    <external-path
        name="external_files"
        path="." />
    
    <!-- External download directory -->
    <external-path
        name="downloads"
        path="Download/" />
    
    <!-- External download subdirectory for our app -->
    <external-path
        name="kefu_updates"
        path="Download/KefuUpdates/" />
    
    <!-- External app-specific files directory -->
    <external-files-path
        name="external_app_files"
        path="." />
    
    <!-- External app-specific cache directory -->
    <external-cache-path
        name="external_app_cache"
        path="." />
    
    <!-- External media directory -->
    <external-media-path
        name="external_media"
        path="." />
    
    <!-- Internal files directory -->
    <files-path
        name="internal_files"
        path="." />
    
    <!-- Internal cache directory -->
    <cache-path
        name="internal_cache"
        path="." />
</paths>