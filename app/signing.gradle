// Groovy script to enable v1 signing — AGP 8.x Kotlin DSL doesn't expose v1SigningEnabled
android {
    signingConfigs {
        release {
            v1SigningEnabled true
            v2SigningEnabled true
        }
    }
}
