// Groovy script to enable v1 signing — AGP 8.x Kotlin DSL doesn't expose v1SigningEnabled
android.signingConfigs.configureEach { config ->
    if (config.name == 'release') {
        config.v1SigningEnabled = true
        config.v2SigningEnabled = true
    }
}
